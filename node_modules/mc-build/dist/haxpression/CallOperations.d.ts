import {ValueType} from "./ValueType"
import {ExpressionType} from "./ExpressionType"
import {Map as Map__1} from "../Map"

export declare class CallOperations {
	protected static map: Map__1<string, {
		arity: number, 
		operation: (arg0: ValueType[]) => ValueType
	}>
	static addFunction(callee: string, arity: number, operation: ((arg0: ValueType[]) => ValueType)): void
	static hasFunction(callee: string): boolean
	static getFunction(callee: string): ((arg0: ValueType[]) => ValueType)
	static getArity(callee: string): number
	static canEvaluate(callee: string, $arguments: ExpressionType[]): boolean
	static evaluate(callee: string, $arguments: ValueType[]): ValueType
	protected static wrapOperation(callee: string, arity: number, operation: ((arg0: ValueType[]) => ValueType)): ((arg0: ValueType[]) => ValueType)
}

import {Map as Map__1} from "../Map"

export declare class McIntIterator {
	constructor(min: number, max: number)
	protected min: number
	protected max: number
	protected current: number
	protected offset: number
	hasNext(): boolean
	next(): number
}

export declare class McFloatIterator {
	constructor(min: number, max: number, step: number)
	protected min: number
	protected max: number
	protected current: number
	protected offset: number
	hasNext(): boolean
	next(): number
}

export declare class Globals {
	static loopVariants: Map__1<any[], ((arg0: any[]) => any)>
	static map: Map__1<string, any>
	static set(name: string, value: any): void
	static get(name: string): any
	static has(name: string): boolean
	static delete(name: string): boolean
}

import {AstNode} from "./AstNode"

export declare class AstStringifier {
	constructor()
	protected segments: string[]
	protected indent: number
	protected tabs: string[]
	protected tab(): void
	protected inc(): void
	protected dec(): void
	protected literal(s: string): void
	protected write(node: AstNode, indent?: boolean, hideBlock?: boolean): void
	protected toString(node: AstNode): string
	static stringify(node: AstNode): string
}

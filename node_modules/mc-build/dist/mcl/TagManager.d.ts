import {CompilerContext, Compiler} from "./Compiler"
import {Map as Map__1} from "../Map"

export declare class TagManager {
	constructor()
	protected tagEntries: Map__1<string, {
		entries: Set<{
			replace: boolean, 
			value: string
		}>, 
		replace: boolean
	}>
	ensureTag(tag: string, context: CompilerContext): string
	addTagEntry(tag: string, entry: string, context: CompilerContext, replace?: boolean): void
	setTagReplace(tag: string, context: CompilerContext, replace: boolean): void
	writeTagFiles(compiler: Compiler): void
}

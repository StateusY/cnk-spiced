import {PosInfo} from "./Tokenizer"
import {McFile, Compiler} from "./Compiler"
import {Map as Map__1} from "../Map"

export declare class LibStore {
	constructor(dir: string)
	protected libDir: string
	protected loadedLibs: Map__1<string, Map__1<string, McFile>>
	lookup(id: string, pos: PosInfo, compiler: Compiler): McFile
	getFilesInDirectory(dir: string): string[]
	protected loadLib(id: string, p: string, compiler: Compiler, pos: PosInfo): null | McFile
}

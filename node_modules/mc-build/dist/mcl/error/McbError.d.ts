import {PosInfo} from "../Tokenizer"
import {Exception} from "../../haxe/Exception"

export declare class McbError extends Exception {
	constructor(msg: string, stack: PosInfo[])
	protected mcbstack: PosInfo[]
	protected __init__(): void
	static isMclError(e: any): boolean
}

import {McbError} from "./McbError"
import {PosInfo} from "../Tokenizer"

export declare class CompilerError extends McbError {
	constructor(message: string, internal: boolean, mcbstack?: null | PosInfo[])
	protected internal: boolean
}

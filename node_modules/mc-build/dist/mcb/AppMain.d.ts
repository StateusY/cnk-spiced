import {Token} from "../mcl/Tokenizer"
import {Compiler} from "../mcl/Compiler"
import {AstNode} from "../mcl/AstNode"

export type DebugFile = {
	a: AstNode, 
	f: string, 
	s: string, 
	t: Token[]
}

export type BuildOpts = {
	baseDir: string, 
	configPath: string, 
	libDir: string, 
	watch: boolean
}

export declare class AppMain {
	static loadDebugProject(file: string, outdir: string): void
	protected static discoverConfigFile(p: string): null | string
	static getFilesInDirectory(dir: string): string[]
	static create(name: string): void
	protected static globalJsData: {
	}
	static compile(opts: BuildOpts): Compiler
	static watch: boolean
	static doBuild(opts: BuildOpts): void
	static generate(outfile: string, arg: {
		baseDir: string, 
		configPath: string, 
		libDir: string
	}): void
}
